<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 站点根 URL
    |--------------------------------------------------------------------------
    |
    | 推广链接 / 分享链接 / 海报二维码 使用的域名。
    | 也可以在后台【系统设置 → 基本设置 → 网站域名】覆盖。
    |
    | 注意：此处 env() 在 config:cache 时会被缓存，不会返回 null。
    |
    */
    'url' => env('SITE_URL', env('FRONTEND_URL', 'http://localhost:5173')),
];
